# Release v0.3.7-alpha — Phase 3 Complete

**Release Date**: 2026-04-20
**Status**: FINAL — APPROVED BY el equipo (2026-04-21)
**Stability**: Alpha (prototype validated, production-ready pending user testing)

---

## 🎯 Summary

PA Framework v0.3.7-alpha marks the completion of **Phase 3** — the Context Loader + Recovery Orchestrator + Modular Knowledge Extraction prototype is fully functional with 285 tests passing.

---

## 🚀 Key Features

### Phase 3 Components

| Component | Description |
|-----------|-------------|
| **Context Loader** | Tiered loading (Tier 0-1 immediate, Tier 2-4 lazy) — ~4.58s cold start |
| **Recovery Orchestrator** | ADR-004 taxonomy + automatic triggers for error recovery |
| **Knowledge Pattern Detector** | Detects session patterns, decisions, pendientes, learnings |
| **Knowledge Extractor v2** | Modular extraction with configurable patterns |
| **Error Logger v2** | Structured error logging with recovery suggestions |

### New Scripts

- `session_autosave.py` — FASE 2: periodic context preservation
- `knowledge_pattern_detector.py` — pattern recognition for knowledge extraction
- `memory_pipeline.py` — Unified memory system (Honcho → Wiki → Session Search)

### UI/UX

- Interactive menu system (la especificación) via `pa.py`
- Dashboard v2.0 with Matrix aesthetic (glass morphism, scanlines, LED indicators)
- Design system CSS with 2026 best practices

---

## 🔧 Fixes & Improvements

### Structural Cleanup

- Removed redundant `skills/` folder (demos not integrated)
- Removed `.opencode/bun.lock` (npm sufficient)
- Consolidated AGENTS.md variants → single industry-standard entry point
- Fixed version mismatches in branding.txt, framework.yaml, package.json

### Configuration

- `config/i18n.json` — Integrated with menu system (ES/EN)
- `config/user-settings.json` — Integrated with preferences system
- `config/framework.yaml` — Updated to v0.3.7-alpha

---

## 📋 AGENTS.md Restructure

**Industry Standard Entry Point**:

| File | Role |
|------|------|
| `AGENTS-lite.md` | Bootstrap mínimo (Tier 0) |
| `AGENTS.md` | Router operativo (Tier 1) |
| `AGENTS-full.md` | Documentación completa (Tier 4) |

**No-overlap policy**: Lite = bootstrap, AGENTS = operación, Full = referencia extensa.

---

## ✅ Tests

```
285 passed
1 skipped
0 failed
```

---

## 📦 Distribution

**ZIP Package**: 877KB / 413 files
**Structure**: Clean, production-ready (pending git push authorization)

---

## 🔜 Next Phase (Phase 4)

- Dashboard merge (Matrix aesthetic + multilenguaje + onboarding no-tech)
- Knowledge base indexing automation
- Cross-platform installer improvements

---

## 🛠️ Hotfix (2026-04-21)

| Archivo | Cambio |
|---------|--------|
| `pa.bat` | Auto-instalación de Python en Windows (winget) y OpenCode (npm + fallback Node.js LTS por winget) |
| `opencode.jsonc` | Corregido al esquema válido de OpenCode: `plugin[]` + `mcp.<server>.type/command[]/environment/enabled` con `context7`, `sequentialthinking`, `github-mcp-server` |
| `core/scripts/persistent_storage_discover.py` | Rutas alineadas a `framework.yaml` (`data/sessions.db`, `core/.context/*`), soporte 4/4 capas aunque estén vacías, y override legacy solo con `PA_FRAMEWORK_USE_STANDALONE_PATHS=1` |
| `core/INIT-PROTOCOL.md` + `docs/MEMORY-ARCHITECTURE.md` + `docs/memory-pipeline/README.md` | Workflow **integral/completo** de 4 capas persistentes (sessions `.md`, Memory MD, SQLite, Wiki), no task-specific |
| `config/framework.yaml` | `memory_pipeline.mode=integral_4layer` + garantías `lossless_capture`, `persistent_all_layers`, `relational_context` |
| `core/scripts/memory_pipeline.py` + `core/scripts/memory_sync.py` | Ciclo interval/session-end integral (incluye `kb_updater` + `memory_sync`) y ruta wiki unificada en `core/.context/knowledge/wiki` |
| `core/memory/{session_memory,user_memory}.py` | Restaurado módulo SQLite real de SessionBridge para persistencia de sesiones con tablas `sessions` y `session_messages` en `data/sessions.db` |
| `AGENTS-lite.md` + `AGENTS.md` + `AGENTS-full.md` | Reintroducido Tier 0 lite y separación semántica sin overlap entre Lite/Router/Full |

---

## 🙏 Credits

**Developer**: FreakingJSON Agencia Team
**Spec Owner**: el equipo
**Phase 3 Lead**: Trinity (dev agent)

---

*PA Framework v0.3.7-alpha — "I own my context. I am FreakingJSON."*