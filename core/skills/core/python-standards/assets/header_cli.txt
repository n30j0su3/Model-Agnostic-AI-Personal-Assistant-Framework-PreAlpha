#!/usr/bin/env python3
"""
Script: {filename}
Description: {description}

This CLI script provides {functionality_description}.
It features automatic environment detection, safe cross-platform output,
and comprehensive error handling.

Usage:
    python {filename} [OPTIONS]
    ./@{filename} --help

Options:
    --input, -i     Path to input file
    --output, -o    Path to output file (default: stdout)
    --verbose, -v   Enable verbose output
    --dry-run       Show what would be done without executing

Examples:
    # Basic usage
    python {filename} --input data.txt

    # With output file
    python {filename} --input data.txt --output result.json

    # Verbose mode
    python {filename} --verbose --input data.txt

    # Dry run
    python {filename} --dry-run --input data.txt --output result.json

Exit Codes:
    0   Success
    1   General error
    2   Invalid input file
    3   Processing error
    4   Output error
    130 Interrupted by user (Ctrl+C)

Author: {author}
Version: {version}
Date: {date}
"""
